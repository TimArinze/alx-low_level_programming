#include "main.h"
/**
 * add - function that adds to integers and returns the result
 *
 * @n: function argument
 *
 * @m: function argument
 *
 * Return: Always return result
 */
int add(int n, int m)
{
	int result;

	result = n + m;
	return (result);
}
