#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry function
 * @argc: parameter
 * @argv: string
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if (argc)
		printf("%s\n", argv[0]);
	return (0);
}
