#include "main.h"
/**
 * _strlen - function that returns the length of a string
 * @s: string to be counted
 * Return: int
 */
int _strlen(char *s)
{
	int a, length;

	for (a = 0; s[a]; a++)
		length = a;
	return (length);
}
