#include "main.h"
/**
 * wildcmp - function that compares two strings and returns 1 if identical
 * and 0 if not
 * @s1: first string to be compare with the second
 * @s2: Second string to be compared with the first
 * Return: 0 or 1
 */
int wildcmp(char *s1, char *s2)
{
	if (*s1 )
