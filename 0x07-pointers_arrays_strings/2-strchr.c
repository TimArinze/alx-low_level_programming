#include "main.h"
/**
 * _strchr - function that locates a
 * character in a string
 * @s: string
 * @c: char
 * Return: char
 */
char *_strchr(char *s, char c)
{
	int i;

	if (s == NULL)
		return (NULL);
	else
		return (s);
}
